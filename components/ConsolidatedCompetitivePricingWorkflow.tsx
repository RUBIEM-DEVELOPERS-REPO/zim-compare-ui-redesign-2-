"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ConsolidatedCompetitivePricingWorkflow() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Consolidated Pricing Workflow</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-center py-8 text-muted-foreground">
          <p>Competitive pricing workflow will be displayed here</p>
        </div>
      </CardContent>
    </Card>
  );
}
